name: main
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose up -d
      - name: main
        run: docker run --network container:flower appropriate/curl -s --retry 10 --retry-connrefused http://localhost:5555/
